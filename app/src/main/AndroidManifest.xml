<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.miprimeraapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".VictoriaActivity">

        </activity>
        <activity android:name=".CajaShakiraActivity"></activity>
        <activity android:name=".CajaActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".ImagenActivity" />
        <activity android:name=".IMCActivity" />
        <activity android:name=".DaLaVueltaActivity">

            <!--
            android:screenOrientation="landscape"
            android:configChanges="screenSize|keyboardHidden|orientation"
            -->

        </activity>
        <activity android:name=".VersionActivity" />
    </application>

</manifest>